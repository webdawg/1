# 1

Early-stage Python project skeleton.

## Layout

- `src/one/` — the `one` package (src layout).
- `tests/` — empty; no test framework configured yet.

## Requirements

- Python >= 3.10

## Install

```bash
pip install -e .
```
